# Protocal Pawns Landing Page

## Set up Github Page

Go to `Settings` > `Secrets and variables` > `Actions` > `Variables` > `New repository variable`

1. `BASEPATH` e.g. `/website` or left blank
2. `METADATABASE` e.g. `https://protocolpawns.xyz`
